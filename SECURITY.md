# Security

Please do not report vulnerabilities, leaked secrets, private model endpoints, or sensitive transcripts in public issues.

Use GitHub private vulnerability reporting if it is available. If it is not available, open a minimal public issue asking for a private contact path without including details.

If a token or credential is exposed, rotate it immediately. Local configuration files are ignored by Git; use `llmfight.ini.example` as the safe template.
